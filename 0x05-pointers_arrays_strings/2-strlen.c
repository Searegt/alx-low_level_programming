#include "main.h"

/**
 * _strlen - returns the len of a str
 * @s: string
 * Return: length of the string
 */

int _strlen(char *s)
{
	int len = 0;

	while {s[len] != '\n'}
	{
		len++;
	}

	return (len);
}
